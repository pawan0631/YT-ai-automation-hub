# .env.template — PRRS Global Enterprise
# Node: [LAPTOP] C:\AI_Agents\.env  |  [VPS] /opt/docker/.env
# NEVER commit the actual .env file — only this template
# Store real values in your password manager (Bitwarden/1Password)
# chmod 600 .env  (on Linux/VPS)
# Updated: 2026-05-31

# ── INFRASTRUCTURE ───────────────────────────────────────────
VPS_IP=YOUR_CONTABO_VPS_IP
TAILSCALE_SUBNET=100.64.0.0/10

# ── LM STUDIO (Laptop only) ──────────────────────────────────
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_MODEL_ID=local-model

# ── DATABASE ─────────────────────────────────────────────────
DB_USER=skillindia_admin
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_MIN_20_CHARS

# ── n8n ──────────────────────────────────────────────────────
N8N_USER=pawi
N8N_PASSWORD=CHANGE_THIS
N8N_ENCRYPTION_KEY=GENERATE_32_CHAR_RANDOM_STRING

# ── DIFY ─────────────────────────────────────────────────────
DIFY_SECRET_KEY=GENERATE_RANDOM_STRING

# ── AI APIs ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...
PERPLEXITY_API_KEY=pplx-...

# ── VIDEO / AUDIO PRODUCTION ─────────────────────────────────
ELEVENLABS_API_KEY=...
HEYGEN_API_KEY=...
HIGGSFIELD_API_KEY=...
RUNWAYML_API_KEY=...
CREATOMATE_API_KEY=...
INVIDEO_API_KEY=...
KLING_AI_API_KEY=...

# ── COMMUNICATION ────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=...
# Get from BotFather: /newbot → name: PRRS Notifications

# ── GOOGLE ───────────────────────────────────────────────────
GOOGLE_API_KEY=...
YOUTUBE_OAUTH_CLIENT_ID=...
YOUTUBE_OAUTH_CLIENT_SECRET=...

# ── AGENT MEMORY ─────────────────────────────────────────────
PAPERCLIP_API_KEY=...

# ── SECURITY NOTES ───────────────────────────────────────────
# 1. Copy this file to .env and fill real values
# 2. Add .env to .gitignore — NEVER commit it
# 3. On VPS: chmod 600 /opt/docker/.env
# 4. Rotate all keys every 90 days
# 5. If any key is exposed, rotate immediately
